class Fibonacci {

    /*
     * Recursive fibonacci function
     */
    fun fibonacci(a, b, N) {
        if N == 0 {
            return null // end of recursion
        }
        print(a)
        return fibonacci(b, a+b, N-1)
    }

    fun main() {
        N = int(args[0])
        fibonacci(0, 1, N)
    }
}
