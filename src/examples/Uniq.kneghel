class Uniq {

    fun main() {
        m = makeDict()
        i = 0
        while i < len(args) {
            if dictGet(m, args[i]) == null {
                print(args[i])
                dictAdd(m, args[i], true)
            }
            i = i + 1
        }
    }
}
