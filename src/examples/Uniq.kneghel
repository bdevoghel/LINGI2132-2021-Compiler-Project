class Uniq {

    fun main(args) {
        m = makeDict()
        i = 0
        while i < len(args) {
            if m[args[i]] == null {
                print(args[i])
                m[args[i]] = true
            }
            i = i + 1
        }
    }
}
